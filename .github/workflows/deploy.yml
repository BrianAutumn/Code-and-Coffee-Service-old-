name: Deploy

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

    steps:
      - name: Get current time
        uses: srfrnk/current-time@master
        id: time
        with:
          format: LLLL
          utcOffset: ${{ secrets.UTC_OFFSET }}
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup node
        uses: actions/setup-node@v1
        with:
          node-version: 14.x
      - name: Get dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Replace tokens
        uses: cschleiden/replace-tokens@v1
        with:
          tokenPrefix: "##"
          tokenSuffix: "##"
          files: '["**/*.js","**/**/*.js","**/*.json","**/**/*.json","**/*.ts","**/**/*.ts"]'
        env:
          BUILD_NUMBER: ${{ github.run_number }}
          BUILD_DATE: ${{ steps.time.outputs.formattedTime }}
      - uses: montudor/action-zip@v1
        with:
          args: zip -qq -r build.zip build
      - name: Create Deploy Bucket
        run: aws s3 mb s3://${{ secrets.S3_BUCKET_DEPLOY_NAME }}
      - name: Upload lambda-package.zip to S3
        run: aws s3 cp build.zip s3://${{ secrets.S3_BUCKET_DEPLOY_NAME }}/lambda-package.zip
      - name: Upload deploy.template.yaml to S3
        run: aws s3 cp deploy.template.yaml s3://${{ secrets.S3_BUCKET_DEPLOY_NAME }}/deploy.template.yaml
      - name: Execute CloudFormation template
        run: aws cloudformation deploy --template-file ./src/cloud-formation/runtime.template.yaml --stack-name ${{ secrets.CLOUDFORMATION_STACK_NAME }} --parameter-overrides DeployBucketName={{ secrets.S3_BUCKET_DEPLOY_NAME }}
      - name: Upload dist to web resources bucket
        run: aws s3 sync dist s3://code-and-coffee-service-web-resources
      - name: Publish SNS topic success
        if: ${{ success() }}
        uses: nothingalike/sns-publish-topic@v1.6
        with:
          MESSAGE: "${{ github.repository }} '${{ github.workflow }}.${{ github.run_number }}' successful"
          TOPIC_ARN: ${{ secrets.SNS_ARN }}
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      - name: Publish SNS topic failure
        if: ${{ !success() }}
        uses: nothingalike/sns-publish-topic@v1.6
        with:
          MESSAGE: "${{ github.repository }} '${{ github.workflow }}.${{ github.run_number }}' failure"
          TOPIC_ARN: ${{ secrets.SNS_ARN }}
        env:
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
